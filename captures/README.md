# Captures

This directory contains the packet capture used for the NGAP/N2 analysis.

## Capture

- `ngap_capture.pcap.pcap` — captured NGAP/SCTP traffic from the Open5GS + UERANSIM experiment.

Open the file with Wireshark and apply the display filter:

```text
ngap
```

The capture is an academic lab artifact. It should be treated as experimental evidence rather than a production-network trace.
